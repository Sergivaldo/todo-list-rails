<div data-controller="dropdown" class="relative">
  <%= button_tag class:"hover:bg-stone-100 hover:font-semibold flex gap-2 items-center p-1", data:{action:"dropdown#toggle click@window->dropdown#hide"} do%>
    <%= render (Icon::IconsComponent.new(icon:'move', size:14)) %>
    <p>Mudar coluna</p>
  <% end %>
  
  <div
      data-dropdown-target="menu"
      class="flex flex-col gap-3 rounded-lg hidden transition transform origin-top-right absolute drop-shadow-lg p-3 left-28 bottom-0 w-full bg-white"
      data-transition-enter-from="opacity-0 scale-95"
      data-transition-enter-to="opacity-100 scale-100"
      data-transition-leave-from="opacity-100 scale-100"
      data-transition-leave-to="opacity-0 scale-95"
    >
    <ul class="flex flex-col gap-2">
      <li>
        <%= button_to 'Pendente', move_to_column_task_path(@task, params:{status:'to_do'})%>
      </li>
      <li>
        <%= button_to 'Executando', move_to_column_task_path(@task, params:{status:'doing'})%>
      </li>
      <li>
        <%= button_to 'Concluído', move_to_column_task_path(@task, params:{status:'done'})%>
      </li>
    </ul>
  </div>
</div>